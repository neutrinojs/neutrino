<% middleware.forEach(({ name, package }) => { %>const <%= name %> = require('<%= package %>');
<% })%>
module.exports = {
  options: {
    root: __dirname,
  },
  use: [<% middleware.forEach(({ name, options }) => { %>
    <%= name %>(<%- options %>),<% })%>
  ],
};
